#define		Seed	99021
#define		B		2		// numAllele

#ifndef MIN 
#define MIN(a, b) ((a) < (b) ? (a) : (b)) 
#endif

#ifndef MAX
#define MAX(a, b) ((a) > (b) ? (a) : (b)) 
#endif

#define		INF			10e7 //10e10

